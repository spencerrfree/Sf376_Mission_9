@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model Book

<div class="card card-outline-primary m-1 p-1">
    <div class="bg-light p-1">
        <h4>
            @Model.Title
            <span class="badge bg-primary" style="float:right">
                <small>$@Model.Price</small>
            </span>
        </h4>
        <div class="card-text p-1">
            <ul>
                <li>Author: @Model.Author</li>
                <li>Publisher: @Model.Publisher</li>
                <li>IBSN: @Model.Isbn</li>
                <li>Classification/Category: @Model.Classification</li>
                <li>Number of Pages: @Model.PageCount</li>
            </ul>

            <form method="post" asp-page="/Cart" class="form-check-inline">

                <input type="hidden" asp-for="@Model.BookId" />
                <input type="hidden" name="returnUrl" value="@ViewContext.HttpContext.Request.PathAndQuery()" />
                <span class="card-text">
                    <button type="submit" class="btn btn-success btn-sm float-end">Add to Cart</button>

                </span>

            </form>
        </div>
    </div>
</div>